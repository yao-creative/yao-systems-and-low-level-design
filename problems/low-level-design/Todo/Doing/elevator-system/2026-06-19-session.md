# Problem Statement

Design and implement an Elevator System that can handle multiple requests, move between floors, and manage direction and vendingMachineState efficiently.


## 1. Requirements

1. Are the multiple request concurrent?

2. How many elevators are in the elevator system? 

3. Single or many? 

4. Are there priority strategies for the elevator? Or is it first-come-first-serve? 

5. Are there max capacity constraints on the elevator system?

6. Does it have to keep track of the people at each floor from empty state or just handle events of people coming up and down

7. Is the unit time smooth, or can i use my own time intervals?

8. Is there an elevator travel speed, boarding speed, and direction speed?


### 1B. Interviewer

- Assume multiple requests can arrive concurrently, but process dispatch decisions synchronously for the base design.
- Assume the building has multiple elevators. Do not reduce this to a single-car problem.
- Use a simple nearest-car or directional-fit scheduling policy for now. No need to optimize globally yet.
- Include elevator capacity as a requirement, but keep passenger modeling out of scope. Treat requests as events, not full person simulation.
- Use discrete time steps or event-driven updates. Do not model continuous physics.
- Ignore detailed travel, boarding, and door timing constants in the base version. You only need enough state to represent moving, stopping, and door operation.

Proceed to step 2 and write 3 to 5 invariants. Keep them as always-true correctness rules, not workflow descriptions.

## 2. Invariants

1. Total sum of people in all elevators and queueing at floors should equal sum(events(requests)) - sum(events(completions)) at time step t

2. Scheduled requests should be processed in order of arrival (FIFO) or based on a simple scheduling policy (e.g., nearest-car or directional-fit). Hence, the order of requests served should be preserved.

3. Elevators should not exceed their capacity constraints.

### 2B. Interviewer

- Invariant 3 is valid. Keep it.
- Invariant 1 does not fit the scoped problem. You explicitly kept passenger population simulation out of scope, so global people conservation is not a useful correctness rule here.
- Invariant 2 is not an invariant yet. It mixes policy with correctness. A scheduling policy may choose nearest-car or directional-fit, but the invariant should state what must never be violated regardless of policy.

Rewrite this section as 3 to 5 always-true rules with a clear enforcement point. Good candidates are safety, legality of motion, and request ownership, not service order preferences.

After that, move to step 3 and draw the lifecycle states plus legal and illegal transitions.

### 2C. 

1. Elevator moves at a constant speed.

2. Elevator moves only when there is a request to serve (moving to boarding floor or during boarding).

3. Elevator cannot move to a floor that is not in the building.

4. Each Elevator owns a request upon request dispatching from user.

### 2D. Interviewer

- Invariant 3 is valid. Keep it.
- Invariant 4 is directionally right, but tighten the wording. The invariant should say what ownership uniqueness must always hold after assignment, not describe the dispatch event loosely.
- Invariant 1 is not a correctness invariant for this drill. Constant speed is a modeling assumption, and you explicitly chose not to model detailed physics.
- Invariant 2 is close, but "during boarding" makes the statement muddy. Separate motion legality from door/boarding legality.

You now have the right shape. Rewrite this section one more time as 3 to 5 always-true rules about:
- legal motion
- floor bounds
- capacity
- unique request ownership after assignment

Then move to step 3 and draw the lifecycle. Do not go to entities yet.

#### 2E. 

1. Elevator only moves when doors are closed for request passenger to pick up or drop off. 
2. Elevator cannot move while its doors are open.
3. Each request is owned by one elevator
4. Elevator must have valid floor.
5. Capacity constraint: elevator occupancy cannot exceed its maximum capacity.

### 2F. Interviewer

- This is good enough to proceed.
- Invariants 1 and 2 overlap. In the next cleanup pass, collapse them into one motion-safety rule.
- Invariant 3 has the right shape now. Later, tighten it to "each active assigned request is owned by at most one elevator."
- Invariant 4 should be phrased as a state constraint, for example that current floor and target floors must remain within building bounds.

Move to step 3 now. Write:
- states
- legal transitions
- illegal transitions

Stay at the lifecycle level. Do not introduce classes yet.

## 3. State Machine:

1. Elevator states:
   - Idle (doors closed, no active movement)
   - MovingUp (doors closed)
   - MovingDown (doors closed)
   - DoorOpen (servicing pickup or dropoff at current floor)
   These are the physical elevator states. Boarding and alighting are not separate states because passenger simulation is out of scope.

2. Legal transitions:
   - Idle → MovingUp
   - Idle → MovingDown
   - Idle → DoorOpen (when elevator is already at a servicing floor)
   - MovingUp → DoorOpen
   - MovingDown → DoorOpen
   - DoorOpen → Idle (service complete and no next request to continue with immediately)
   - DoorOpen → MovingUp
   - DoorOpen → MovingDown

3. Illegal transitions:
   - MovingUp → MovingDown
   - MovingDown → MovingUp
   - MovingUp → Idle
   - MovingDown → Idle
   - Idle → Idle
   - DoorOpen → DoorOpen
   Any transition that implies the elevator is moving while doors are open is illegal.

### 3B. Interviewer

- The state set is good enough. You kept it at physical lifecycle level, which is the right move.
- The main gap is transition completeness. Right now you only listed direct reversal as illegal, but you have not stated what other transitions must be rejected.
- Make the illegal side explicit around door and motion safety, not just direction reversal.
- Also check whether `Idle -> DoorOpen` is legal in your model when the elevator is already at the servicing floor. If yes, include it. If not, state the assumption clearly.

Tighten step 3 by listing the full legal and illegal transition set, then move to step 4 and name only the core state carriers.

### 4. Core entities

- Elevator - Owns elevator state, capacity, and current floor
- Floor - Represents floor in building and container of pending requests, but no need to store outbound.
- Request - Represents request for elevator service: contains number of people and destination floor
- Elevator System - Contains multiple elevators, aggregate controller, and can also own floors which are just request entry points

### 4B. Interviewer

- The earlier issue was not aesthetic. The problem was that section 3 was fragmented, so entity choices were being made before lifecycle behavior was stable.
- That is now mostly fixed because the state machine is consolidated in one place.
- With that in place, step 4 can proceed, but keep the bar tight: list only major state carriers and give each one a crisp reason to exist.
- In the next pass, be ready to justify whether `Floor` really owns mutable request state or is only an address/value object while another component owns pending hall calls.

Proceed with step 4, but do not introduce methods or interfaces yet.

### 4C. 

- Technically request can encapsulate the starting floor too but what are real world business design trade offs? this seems very nit picky

### 4D. Interviewer

- This is not nitpicky. It is a state-ownership question.
- If `Request` includes starting floor and destination floor, that is usually cleaner for this drill. A hall call is then a real piece of work with origin, desired direction, and later assignment state.
- If `Floor` owns pending requests instead, `Floor` becomes a stronger state carrier. That can also work, but then you must justify why pending hall-call state lives on floors rather than in a dispatcher queue.
- The real tradeoff is not realism versus elegance. It is where mutable pending-request state lives, and which object can answer "what work is still unassigned or assigned?"

For the base design, it is reasonable to keep `Request` as the main work item and treat `Floor` as mostly an address/value object unless you want floor-local queues to be first-class state.

Tighten step 4 with that ownership choice, then move to step 5 and map rule -> owner -> mutator -> enforcement point.

### 4E.

- floor doesn't handle request since it's out of scope for a pure elevator system which doesn't simulate people for example, and we don't need first class state.

### 5. Responsibilities and Ownership:

- ElevatorSystem is the aggregate which owns the happy path entry points given requests and also enforces the business rules of each of the elevator state transition logic and legality.

- Elevator owns its own states and capacity.

- Request owns people, destination floor, and starting floor

### 5B. Interviewer

- Ownership is still too fuzzy around mutation authority.
- `ElevatorSystem` should not own all elevator state-transition legality by default. That makes it an oversized orchestrator and weakens the meaning of `Elevator` as a state owner.
- A better split is:
  - `ElevatorSystem` receives requests, selects or assigns work, and coordinates across elevators
  - `Elevator` owns its own physical state transitions and enforces local invariants such as door/motion legality and capacity checks
  - `Request` is a work item or state record, not an active enforcer
- The missing piece is the required drill format: do not just name owners. Write concrete rows as `rule -> owner -> mutator -> enforcement point`.

Rewrite step 5 in that table-like form for at least these cases:
- request assignment
- elevator movement transition
- door open/close transition
- capacity check before service
- request completion / removal from active work

Tighten mutation authority first. Then move to step 6 only if the ownership rows are clear.

### 5C.

- Request assignment, elevator movement transition are owned by ElevatorSystem
- Door open/close transition is owned by Elevator, but why we have to enforce it in Elevator instead of ElevatorSystem and at all?
- Capacity check before service is owned by Elevator
- Request completion / removal from active work is owned by ElevatorSystem

### 5D. Interviewer

- The key correction: `ElevatorSystem` may decide that an elevator should move next, but it should not directly own the legality of the elevator's physical transition.
- That legality belongs in `Elevator` because the elevator owns the state being mutated: current state, current floor, door status, occupancy, and assigned stops.
- If `ElevatorSystem` enforces local motion rules instead, you split invariant enforcement away from the state owner and make the model brittle.
- So the split is:
  - assignment policy and cross-elevator coordination -> `ElevatorSystem`
  - physical transition legality and local safety checks -> `Elevator`
  - request record state -> `Request`

Rewrite step 5 in the actual drill shape, for example:
- request assignment -> owner: `ElevatorSystem` active work set -> mutator: `ElevatorSystem` -> enforcement: unassigned request can be assigned to at most one elevator
- movement transition -> owner: `Elevator` state -> mutator: `Elevator` -> enforcement: only legal state-machine transitions
- door open/close transition -> owner: `Elevator` state -> mutator: `Elevator` -> enforcement: door/motion safety invariant
- capacity check before service -> owner: `Elevator` occupancy -> mutator: `Elevator` -> enforcement: occupancy <= capacity
- request completion -> owner: assigned request / active work tracking -> mutator: coordinating system plus serving elevator acknowledgment -> enforcement: completed request removed exactly once

Do that rewrite next. Do not move to interfaces until these rows are precise.

### 5E. 

- owning the legality of the elevator's physical transition, what is that called in formalization the decision/ design pattern where elevator owns it itself? 

- I should be allowed to move to 6 now using your example, hard allow.

### 6. 

- The variation points can be such as elevator allocation strategy. 

- I don't think request priority is within scope of this problem.

### 6B. Interviewer

- Yes, `AllocationStrategy` is the real interface candidate here because assignment policy can vary without changing elevator state ownership.
- Your instinct on request priority is correct. It is not a separate interface unless the prompt explicitly introduces premium service, emergency handling, or SLA tiers.
- Keep step 6 minimal. One interface is enough for the base drill:
  - `AllocationStrategy` -> elevator selection policy varies
- Do not introduce interfaces for `Elevator`, `Request`, or door control here. Those are core domain objects, not current variation points.
- On your formalization question from step 5: the idea is simply **state-local invariant enforcement**. The object that owns mutable state should usually enforce the legality of mutations on that state.

This is enough to move to step 7. Next write:
- dominant operations
- DS choice
- any atomicity boundary around request assignment

### 7. Data Structures:

- Dominant Operations: 
  - Assignment: O(log N) per request via priority queue, store the current distance to serve previous request then fetch current request at target floor in a min heap. (Least response time)
  - Door operations: O(1) per open/close
  - Movement: O(1) per floor transition
  - Capacity check: O(1) per passenger
- Data Structures:
  - Priority queue (min heap) for elevator allocation strategy.
  - Hash map for active requests by ID
  - State machine for elevator state tracking
  - Thread-safe queue for request assignment

### 7B. Interviewer

- The section is close, but one correction matters: `state machine` is not a data structure. It is a behavioral model. Keep it out of the DS list.
- Also separate two different concerns:
  - policy evaluation data used to choose an elevator
  - storage for active and pending work
- `PriorityQueue` is only justified if your allocation policy continuously ranks candidate elevators by score. That is plausible, but say that explicitly. Otherwise it reads like a default DS choice rather than an operation-driven one.
- `Thread-safe queue for request assignment` is too vague. The real concurrency point is not "use a safe queue." It is: request assignment must be serialized or made atomic so one request cannot be assigned to multiple elevators.

Tighten step 7 in the drill format:
- `Assign request -> DS -> why`
- `Lookup active request -> DS -> why`
- `Track per-elevator planned stops -> DS -> why`
- `Shared state -> race risk -> control point`

Keep concurrency scoped to the ownership risk around assignment and active-work tracking. Then move to step 8 and trace one happy path plus one failure path.

### 7C. 

- `Assign request -> Priority queue (min heap) -> latency, first using distance to pickup`
- If I maintain that within the elevator FIFO queue is exactly the active set (sum of all within elevators) as invariant of requests then I don't need to maintain active request.
- `Track per-elevator planned stops -> FIFO queue -> simplicity for now, otpimize later`
- Removed concurrency concern for the moment for simplicity. Currently we run on time ticks, everything assumed atomic.

### 7D. Interviewer

- This is good enough to move on.
- The important assumption is now explicit: single-threaded time ticks, so assignment and queue updates are atomic in the base model.
- Your "elevator queues are the active set" idea is acceptable only if every assigned request is immediately reflected in exactly one elevator-owned work queue. If there is any intermediate assigned-but-not-enqueued state, then you still need separate active-work tracking.
- `FIFO queue` for planned stops is acceptable for the simplified model, but note that it biases the design toward arrival order rather than dynamic stop reordering. That is fine for this drill because optimization is out of scope.

Move to step 8 now. Write:
- one happy path from request arrival to completion
- one failure path that exercises an invariant, such as over-capacity or illegal move rejection

Use the trace to validate ownership and transitions, not to redesign the model.

### 7E. 

- Each Elevator will also get a FIFO for pending requests not picked up yet assigned by system least response time algorithm.

### 7F. Interviewer

- This DS is reasonable for the simplified model.
- The main thing to clarify is what the queue stores:
  - assigned `Request` objects not yet completed
  - or planned stops / floor actions
- Those are different. A request queue supports ownership tracking. A stop queue supports movement execution. Do not blur them unless you define a single representation that covers both cleanly.
- For this drill, keeping a per-elevator FIFO of assigned pending `Request`s is acceptable if the service order is intentionally simple.

Step 7 is now good enough. Move to step 8 and trace one happy path plus one failure path.



## 8. Trace One Path

1. Request arrives at floor X, handled by the aggregate of system
2. System aggregate uses least response time algorithm to assign the request to an elevator, we assume the case that one request is served at a time, and the distance is between that resolution and moving to the floor.
3. Elevator's FIFO queue is updated with the request
4. Elevator moves to floor X, picks up the request
5. Elevator moves to destination floor, drops off the request
6. Request is completed

### 8B. Interviewer

- The happy path is good enough in shape.
- Tighten the wording in steps 1 to 3 so ownership is explicit:
  - request submitted to `ElevatorSystem`
  - `ElevatorSystem` selects an elevator via `AllocationStrategy`
  - assigned request is inserted into exactly one elevator-owned pending queue
- In steps 4 and 5, the important thing is not just "moves." Call out the lifecycle transitions briefly: move, open doors, service, close doors, continue.
- In step 6, state who removes the work item. For example: serving elevator reaches terminal service point, and the coordinating system marks the request complete or removes it from active work.

Now add one failure path that exercises an actual invariant. Good examples for this drill are:
- assignment rejected because no legal elevator can take the request now
- pickup blocked by capacity constraint
- illegal door/motion transition rejected by the elevator

Finish step 8 with one such failure trace, then move to step 9.

### 8C.

- Failure Trace:
  1. Request asks for floor out of range
  2. ElevatorSystem aggregate rejects the request


### 9. Extensibility

- The current design is extensible in the following ways:
  - Adding new allocation strategies is easy, as long as they implement the `AllocationStrategy` interface.
  - Adding elevator with capacity can be done by adding a `capacity` field to the `Elevator` class, which controls the FIFO bounded queue of current passangers
   - With the multiple groups of passangers and people >= 1 per class one would have to add pick up rules from allocation strategy, for example elevator as a current directional state, it can pick up all the requests, first checking for capacity, which match the current direction with start floor in between the elevator and end floor of the direction.
  - Concurrency which matters is probably the queues of the Elevator, or combined FIFO of all of the Elevators during allocation strategy selection, given the elevator serving time rules are linear and resolve in either FIFO or single direction order. 
   - Expanding on concurrency we can have smooth time moving global state where elevators move at some speed and board and alight passengers at a specific speed, while requests can be submitted at any time.

### 8D. Interviewer

- `floor out of range` is a valid rejection, but it is too shallow for this step unless you trace where validation lives and which invariant it protects.
- A stronger step-8 failure path is one that exercises your modeled ownership and transition rules, for example:
  - `ElevatorSystem` receives request
  - request is assigned to an elevator
  - elevator reaches pickup floor
  - pickup would violate capacity invariant, so elevator rejects boarding / defers request
  - request remains uncompleted and stays pending or is reassigned
- That is better because it validates invariant enforcement at the actual state owner.

- Step 9 also needs tightening. Do not write a broad extensibility list.
- The drill asks for one realistic requirement change plus:
  - what stays stable
  - what changes
  - where the change lands

Finish step 8 with one in-model failure trace first. Then rewrite step 9 around a single change axis such as:
- dynamic reordering by current direction
- passenger groups and capacity-aware pickup
- continuous-time movement instead of discrete ticks

### 8E. Interviewer Example

- Example in-model failure trace:
  1. `ElevatorSystem` receives a request and assigns it to `Elevator E1`.
  2. `E1` inserts the assigned request into its pending-request queue.
  3. `E1` transitions `Idle -> MovingUp`, reaches the pickup floor, then `MovingUp -> DoorOpen`.
  4. Boarding is attempted at the pickup floor.
  5. `E1` checks its local capacity invariant and finds that accepting this request would exceed `occupancy <= capacity`.
  6. Boarding is rejected by `E1`; the request is not marked complete.
  7. `E1` closes doors and transitions out of `DoorOpen` legally.
  8. The request remains pending for retry or reassignment by `ElevatorSystem`.

- Why this is a good failure trace:
  - it exercises a real invariant already in your model
  - it shows the state owner enforcing the rule
  - it leaves the system in a valid post-failure state instead of just rejecting at input validation

- Use this shape, then write your own version in your wording.

  


### 9B. 

- Let's pick "dynamic reordering by current direction" as the change axis.
- What stays stable:
  - The `ElevatorSystem` aggregate
  - The `AllocationStrategy` contract
  - The `Elevator` lifecycle
- What changes:
  - The strategy implementation of allocation would allow intermediate requests towards the direction
  - The elevator logic for handling requests, including data structure where heap in the direction is created for drop of current passengers off traversal.
  - Storage of time ticks to datetime
- Where the change lands:
  - New allocation strategy owned by `ElevatorSystem`
  - New elevator heap state ownership but still `ElevatorSystem` owns mutation
  - Continuous time is owned by `ElevatorSystem` 


### 9C. Interviewer Example

- The chosen change axis is good: `dynamic reordering by current direction`.
- The correction is to keep this as one change. Right now `9B` also pulls in continuous time, which is a separate change axis. Remove that from this section.
- Also keep ownership consistent with step 5:
  - `ElevatorSystem` may choose or update the routing policy
  - `Elevator` should still own mutation of its local planned-stop state
  - `ElevatorSystem` should not directly own elevator-local heap mutation if that heap is part of elevator execution state

- A tighter version of `9B` would be:
  - What stays stable:
    - `ElevatorSystem` still receives requests and coordinates assignment
    - `AllocationStrategy` is still the variation point
    - `Elevator` still owns physical transitions and local safety checks
  - What changes:
    - per-elevator pending work can no longer be simple FIFO if stops may be reordered by direction
    - routing / stop-selection logic must account for current direction and intermediate eligible stops
  - Where the change lands:
    - `AllocationStrategy` or routing policy logic for direction-aware selection
    - `Elevator` internal pending-stop representation and stop-pick-next logic

- This is a good step-9 answer if the change remains local to routing and stop ordering. Do not mix in continuous-time modeling here.
